version: '3'
services:
  db:
    build:
      context: ./docker
      dockerfile: mysql.dockerfile
    ports:
    - "3306:3306"
    container_name: meu_db
    volumes:
    - /home/marcos/docker/mysql_volume:/var/lib/mysql